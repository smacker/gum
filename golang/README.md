# golang parser integration package

The main goal of this package is to validate that gum API can be used with different parsers.
It's incomplete implementation which skips some properties of the nodes.
